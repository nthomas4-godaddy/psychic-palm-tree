/**********************************************************
 * @param {number} a - The first number
 * @param {number} b - The second number
 * @returns {number} The sum of a, b, and uber
 **********************************************************/
const uber: number = 0;

function sum(a: number, b: number): number {
  console.log(a, b);
  return a + b + uber;
}